import { PipeElevadoPipe } from './pipe-elevado.pipe';

describe('PipeElevadoPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeElevadoPipe();
    expect(pipe).toBeTruthy();
  });
});
